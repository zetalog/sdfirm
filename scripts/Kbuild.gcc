AS		= $(CROSS_COMPILE)as
LD		= $(CROSS_COMPILE)ld
CC		= $(CROSS_COMPILE)gcc
CPP		= $(CROSS_COMPILE)cpp
AR		= $(CROSS_COMPILE)ar
NM		= $(CROSS_COMPILE)nm
STRIP		= $(CROSS_COMPILE)strip
OBJCOPY		= $(CROSS_COMPILE)objcopy
CPPFLAGS        := -D__KERNEL__ $(TARGETINCLUDE)
CFLAGS          := -Wall -DKBUILD_CONF $(TARGETINCLUDE)
AFLAGS          := -D__ASSEMBLY__
SYMLINK		:= cp -rf
#SYMLINK	:= ln -fsn
